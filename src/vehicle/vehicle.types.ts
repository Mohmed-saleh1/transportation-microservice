export enum VehicleType {
  BUS = 'bus',
  TAXI = 'taxi',
  TRUCK = 'truck',
  VAN = 'van',
}

export enum VehicleStatus {
  AVAILABLE = 'available',
  IN_TRANSIT = 'in-transit',
  MAINTENANCE = 'maintenance',
}
